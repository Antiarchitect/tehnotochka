<% total = ((count = count_products(taxon)) > 4 ? " (#{count} моделей)" : "") %>
<div class="taxon">
  <h2><%= link_to "#{taxon.name}<span style='font-size: 18px;'>#{total}</span>", seo_url(taxon), :class => 'breadcrumbs' %></h2>
  <%= render :partial => "shared/products.html.erb", :locals => {:products => taxon_preview(taxon), :taxon => taxon} %>
</div>